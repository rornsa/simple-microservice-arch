[project]
name = "notification-service"
version = "0.1.0"
description = "Notification service to notify students about success events"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aio-pika>=9.6.1",
    "fastapi[standard]>=0.133.1",
    "pydantic-settings>=2.13.1",
]
